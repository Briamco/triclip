{
  "name": "triclip",
  "version": "1.0.0",
  "private": true,
  "description": "Precision video trimming web application",
  "scripts": {
    "dev": "pnpm run dev:server & pnpm run dev:client",
    "dev:server": "cd server && pnpm run dev",
    "dev:client": "cd client && pnpm run dev",
    "build": "cd client && pnpm run build",
    "install:all": "cd server && pnpm install && cd ../client && pnpm install"
  }
}
